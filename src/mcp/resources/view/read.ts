import { toHandle } from '../utils';

/*
columns
TODO: What other things can you get about views?
*/
async function readViewResource(uri: string, schema: string, name: string): Promise<string> {
  return '';
}

export default readViewResource;
